[project]
name = "anime-with-her-worker"
version = "0.1.0"
description = "Worker for AI dubbing director console"
requires-python = ">=3.12"
dependencies = [
  "pydantic",
  "pytest"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

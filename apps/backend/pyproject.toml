[project]
name = "anime-with-her-backend"
version = "0.1.0"
description = "Backend for AI dubbing director console"
requires-python = ">=3.12"
dependencies = [
  "fastapi",
  "uvicorn",
  "pydantic",
  "pytest"
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

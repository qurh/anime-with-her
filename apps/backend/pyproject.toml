[project]
name = "anime-with-her-backend"
version = "0.1.0"
description = "Backend service for anime-with-her"
requires-python = ">=3.11"
dependencies = [
  "fastapi>=0.135.3",
  "uvicorn>=0.30.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]